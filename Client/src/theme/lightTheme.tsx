import { createTheme } from "@nextui-org/react"

export const lightTheme = createTheme({
    type: "ligth",
    theme: {
        colors: {
            // primary: "$white",
            primary_white: "$white",
            secondary: "$blue500",
            background: "#EDEDED",
            primaryBorder: "$white",
            primaryBorderHover: "$white",
        }
    }
})
